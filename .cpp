#include <iostream>
using namespace std;

class invoice {
private: 
	string number;
	string description;
	int quantity;
	int price;
public:
	Invoice(string, string, int, int); //I believe this is the constructor. I'm not exactly sure what the use of it is.
	//Unsure what to put here
	void GetNumber(number);
	void GetDescription(description);
	int GetQuantity(quantity);
	int GetPrice(price);


};

void main() {

};